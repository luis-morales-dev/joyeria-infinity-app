import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mx.joyeriainfinity.app',
  appName: 'joyeria-infinity-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
